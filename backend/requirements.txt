Django==5.0.4
django-ninja==1.6.2
djangorestframework==3.15.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.3.1
python-dotenv==1.0.1
requests==2.32.5
beautifulsoup4==4.14.3

pypdf==6.12.2
python-docx==1.2.0
python-pptx==1.0.2
PyMuPDF==1.27.2.3

pytesseract==0.3.13
pdf2image==1.17.0
Pillow==11.3.0

sentence-transformers==5.5.1
chromadb==1.5.9
groq==0.37.1

google-api-python-client==2.197.0
google-auth==2.53.0
google-auth-oauthlib==1.4.0

cryptography==43.0.3
# openai>=1.60 and anthropic>=0.40 required - older pins construct their
# internal httpx.Client with a `proxies` kwarg that httpx>=0.28 (already
# required by other packages in this environment) removed.
openai>=1.60.0
anthropic>=0.40.0
# Gemini's ai_provider client talks to the Generative Language REST API
# directly via `requests` rather than the google-generativeai SDK - that
# SDK's API key is set via a process-global genai.configure() call, unsafe
# when concurrent requests from different workspaces use different keys.
